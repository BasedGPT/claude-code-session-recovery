---

## 2026-05-20 — Lexicon cleanup of user-facing problem strings

**What we worked on:** Updated all six `problem` strings (and their mirrors in README.md and troubleshooting.md) to use canonical vocabulary from `SHARED-LEXICON.md`. Also fixed four additional violations found in broader scans of the same files.

**What was completed:**
- All 6 `problem` fields in `troubleshooting.json` — stale terms replaced with lexicon-clean strings
- `README.md` symptom table — 6 matching cells updated; also fixed intro line ("history is blank" → "session list is blank") and "Where to start" nav entry
- `troubleshooting.md` — 6 Symptom fields updated; Root cause field for `duplicate-synth-metadata` fixed ("history entries" → "sessions in the session list"); orphan Root cause field fixed ("history panel" → "session list")
- 10 fixture golden `diagnose.txt` files updated (task brief said 8; actual count was 10 — fixtures 04, 08, 09, 12 also carry these strings)
- All 13 fixture tests pass throughout

Key string changes:
- `blank-pane-missing-cli`: "blank pane" / "history" → "open with no conversation history"
- `cli-points-missing-jsonl`: "session in history" / "conversation content" → "conversation history is missing from disk"
- `duplicate-synth-metadata`: "history point to the same conversation" → "open to the same conversation history"
- `old-root-cwd-reference`: "show history but are listed under" → "started from a bare drive root"
- `junction-realpath-slug-mismatch`: "show separate history from sessions at the real path" → "One project shows two sets of sessions in the session list"
- `orphan-jsonl-no-metadata`: "does not show some sessions in history at all" → "absent from the session list"

**Where we stopped:** All three explicitly targeted surfaces clean and tests passing. The broader docs (`docs/session-recovery.md`, `docs/architecture.md`, `AGENTS.md`, `SECURITY.md`, `CONTRIBUTING.md`) were not scanned.

**What's next:** Decide whether to run a full repo-wide lexicon scan. The task brief said those docs were "already lexicon-clean" — but the same was said of README.md and troubleshooting.md, which had 4 violations. If full lexicon discipline is the goal, a single grep pass across the remaining docs is the right call. Entry point: run the banned-term grep from `SHARED-LEXICON.md` against `docs/` and root-level `.md` files.

**Key decisions made:**
- Scope expanded (with approval) beyond the 6 targeted strings to fix all violations found in touched files
- Mode 5 symptom simplified to "One project shows two sets of sessions in the session list" — the earlier trailing clause ("or some sessions appear missing") was dropped; implicit, but the new string is clearly correct
- `troubleshooting.md` Symptom fields kept as sentences with periods (matching existing convention) rather than made identical to the `problem` fields

**Open questions / decisions pending:** Full repo-wide lexicon scan — yes or no?

---

## 2026-05-21 — claude-code-session-recovery: Phase 2.11 final QA + public launch

**What we worked on:** Executed the Phase 2.11 prompt (`prompts/2026-05-21-phase-2-11-final-qa-launch.md`) — full pre-launch QA pass followed by opening the tracking issue and flipping the repo public.

**What was completed:**
- All local QA gates passed: 13/13 fixture tests, diagnose.py smoke test clean, all 5 workflow YAMLs valid, issue-labeler has zero `(?i)` flags, sanitisation-patterns intact, all 5 doc files passed their content gates.
- CI confirmed green by Robbie (fixture-tests + sanitisation-lint on `bbf27bb`).
- Tracking issue #1 opened: https://github.com/BasedGPT/claude-code-session-recovery/issues/1
- Discovered the labeler CI went red on issue #1 — five domain labels (`domain:session`, `domain:worktree`, `bug-class:junction-realpath`, `topic:backup`, `symptom-not-covered`) hadn't been created. Created them; labeler will work on future issues.
- Repo flipped PUBLIC. Confirmed `PUBLIC` via `gh repo view`.
- COWORK_PROJECTS.md updated: phases 2.10 and 2.11 marked complete, status updated to public.
- Spawned a chip to fix the three live session issues detected on Robbie's machine (blank-pane-missing-cli, cli-points-missing-jsonl, junction-realpath-slug-mismatch).

**Where we stopped:** Repo is public. Tracking issue is open. Live session repair chip spawned (not yet executed in this session).

**What's next:** Two things.
1. *Concrete task:* Comment on 3–5 existing user issues on `anthropics/claude-code` that match the symptom table (blank pane, session not found on disk, etc.) — link to `BasedGPT/claude-code-session-recovery`. This was the planned post-launch distribution step. Also the r/ClaudeAI post.
2. *Pending:* Live banner-quietness test for the sparse-checkout stub fix (from the worktree project, still unchecked in COWORK_PROJECTS.md).

**Key decisions made:**
- Flipped repo public once CI confirmed green and labeler was fixed — didn't wait for a separate confirmation step.
- Windows installer deferred: bootstrap script (`.ps1`) is right for v1; standalone `.exe` only if users open issues about missing Python.
- Label colours: domain:session → 0052cc, domain:worktree → 1d76db, bug-class:junction-realpath → e4e669, topic:backup → bfd4f2, symptom-not-covered → d93f0b.

**Open questions / decisions pending:**
- Memory candidate not approved/rejected: "GitHub issue-labeler labels must be pre-created before any issue is opened, or CI goes red." Propose at next wrap-up if Robbie wants to save it.
- The auto-snapshot hook committed `dcd2741 feat(groupings)` during the session from the project repo. Worth checking whether the hook is scoped correctly (should only run in Cowork, not in project repos).
- Repo slug `BasedGPT/claude-code-session-recovery` confirmed; but the "repo slug confirmation" step in the original plan was checking whether the intended public name matched. Considered done.

